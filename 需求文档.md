# AgentFlow 需求文档

> **用途**：本文档是 AgentFlow（v0.1，2026-08-29 止于 commit `768bba9`）的完整需求规格，供移植/集成到另一个现有项目时作为需求输入使用。需求条目按模块编号（FR-xx / NFR-xx），标注优先级（P0 核心 / P1 重要 / P2 可选），并单列「集成必须保留的架构不变量」与「明确不做的事」。
>
> **依据**：`docs/superpowers/specs/2026-08-27-agentflow-design.md`（基础设计）、`docs/superpowers/specs/2026-08-28-provider-agent-ecosystem-design.md`（供应商×Runtime×Skill×MCP 重构）、`docs/用户手册.md`、实际代码（节点注册表 20 类）。

---

## 1. 产品概述

### 1.1 背景与问题

AgentFlow 解决两个相互关联的问题：

1. **多 Provider API Key 集中管理**：开发者手中往往有同一供应商的多个 Key（个人 + 团队 + 试用）。传统工具（如 cc-switch）只能「全机器同一 Key」地切换；AgentFlow 允许按档案池化管理，按策略（加权/优先级）并发分配、自动健康惩罚与 failover。
2. **多 Agent 工作流可视化编排**：把「用 Gemini 写设计、用 GLM 实现代码、用 Grok 生图、用本地 Claude Code 干活」这类多步骤协作画成节点连线图（ComfyUI 风格），一键运行、全程可观。

### 1.2 产品定位

- **形态**：Tauri 2 桌面应用（Linux x64/arm64、Windows x64 为主力环境），同时保留**浏览器模式**（`pnpm dev` 即得完整编辑能力，零系统依赖）。
- **双引擎**：同一套 UI，运行时自动选择后端——浏览器走 TS 内置引擎，桌面走 Rust 引擎。
- **执行模式**：完全内置。LLM/生图直连各 Provider API；Agent 节点在本机以 headless 方式驱动真实 agent CLI（Claude Code / Codex / Grok Build / dsh / 自定义）。

### 1.3 目标用户与典型场景

目标用户：同时使用多家 LLM 供应商、持有多个 Key、需要编排多步骤 AI 任务的开发者/技术用户。

| 场景 | 流程示例 |
|---|---|
| 游戏开发 | 设计文档（LLM）→ 代码实现（另一家 LLM）→ 美术素材（生图）→ 测试用例（agent CLI） |
| 内容生产 | 选题 → 结构化输出选型 → 长文 → 模板渲染改写成多平台推文 |
| 数据分析 | HTTP 拉数 → JS 清洗 → LLM 解读 → 写文件落盘 |
| 本地 agent 编排 | 多个 agent 节点各绑不同 CLI × 供应商档案 × Skills × MCP，接力完成工程任务 |

---

## 2. 总体架构需求（集成必读）

### 2.1 双引擎架构（P0）

前端 UI **只依赖**统一的后端抽象接口 `Backend`（本项目位于 `src/backend/types.ts`），运行时自动二选一：

| | 浏览器引擎（TS） | 桌面引擎（Rust / Tauri） |
|---|---|---|
| 持久化 | localStorage + OPFS | SQLite（rusqlite）+ OS keyring |
| LLM/生图直连 | fetch（吃 baseUrl） | reqwest（吃 baseUrl，支持系统代理） |
| agent 节点 | 可编辑/校验，执行=blocked + 明确提示 | headless 子进程真实执行 |
| 代码沙箱 | JS：Web Worker（无 DOM）；Python：Pyodide WASM | 本地子进程（超时强杀、输出限 1MB） |
| 文件落盘 | OPFS 沙箱磁盘 | 应用数据目录真实文件 |
| Skills 本机扫描 | 仅自建库（OPFS） | 四根目录扫描 + 自建库 |
| Runtime 安装 | 引导态（「需桌面版」） | 检测/安装/更新 |

**架构铁律（不变量，见 §9）**：数据模型、调度语义、执行语义在 TS 与 Rust 两侧**同语义同步实现、双侧测试**，只改一侧会分叉。

### 2.2 技术栈基线（P1，可按目标项目替换，但需保住等价能力）

| 层 | 本项目选型 |
|---|---|
| 外壳 | Tauri 2.x（**已明确否决 Electron，勿再提**） |
| 后端 | Rust + Tokio；纯核心库 `agentflow-core` 可独立 `cargo test` |
| 前端 | React 18 + TypeScript + Vite + Tailwind |
| 画布 | React Flow (xyflow) |
| 状态 | Zustand |
| HTTP | reqwest + eventsource-stream（SSE 流式） |
| 密钥 | keyring crate（Linux secret-service / Windows wincred） |

---

## 3. 功能需求

### FR-1 供应商档案管理（ProviderProfile）（P0）

**数据模型**：

```ts
interface ProviderProfile {
  id: string;
  name: string;              // 显示名，如「Anthropic 官方主力号」
  presetId: string;          // 内置预设 id 或 'custom'
  protocol: 'openai-compat' | 'anthropic' | 'gemini';
  baseUrl: string;           // 必填，预设自动填充可改
  apiKey: string;            // secret，存 keyring/localStorage
  models: ModelEntry[];      // 获取 + 勾选 + 手动兜底
  weight: number;
  isDisabled: boolean;
  note?: string;
  createdAt: number;
  health: KeyHealthSnapshot; // 调度健康快照
}
interface ModelEntry { id: string; visible: boolean; source: 'fetched' | 'manual'; }
```

需求条目：

- **FR-1.1 同供应商多档案**：同一预设可建多条档案（= 多 Key 并存），每条独立启用/禁用、独立备注。同一协议族的中转档案每条可自成调度组。
- **FR-1.2 内置预设**（选预设自动带 baseUrl/端点，可改）：
  - 官方：智谱 GLM、智谱 GLM (Anthropic 兼容 `https://open.bigmodel.cn/api/anthropic`)、Kimi、Kimi (Anthropic 兼容 `https://api.moonshot.cn/anthropic`)、Gemini、Anthropic、xAI Grok、OpenAI、DeepSeek、DeepSeek (Anthropic 兼容 `https://api.deepseek.com/anthropic`)
  - 国内官方：智谱开放平台、火山方舟、火山方舟 Coding Plan (Anthropic 兼容)、SiliconFlow、SiliconFlow (Anthropic 兼容)、MiniMax (Anthropic 兼容)、阿里百炼 Coding Plan (Anthropic 兼容)
  - 自定义中转：仅填 baseUrl + 协议 + key，不猜端点
  - 预设字段：`{id, name, category, protocol, baseUrl, modelsUrl?, modelsAuth?, apiKeyUrl?, accent}`；分类 chips：全部/官方/国内官方/自定义，「自定义中转」虚线卡常驻尾部
- **FR-1.3 Base URL 语义**：只填到根（如 `/v1`），**不带** `/chat/completions` 完整端点；URL 拼接统一走 `joinUrl`/`join_url` 双侧同语义纯函数（base 去尾斜杠；相同版本段如 `/v1` 只去重一次；`/v4` ≠ `/v1` 不去重）。
- **FR-1.4 key 掩码与按需回显**：列表接口只回掩码（`maskSecret`：空或 ≤8 位 → `***`；否则前 3 + `***` + 后 4）；`revealProviderSecret(id)` 仅在用户明确进入编辑/复制动作时按档案回明文；编辑弹窗预填真实值（password 型 + 眼睛切换）；**留空保存 = 保留原 key**；「复制档案」连密钥一起携带。
- **FR-1.5 连通测试**：按钮先保存当前表单再测试（测的是屏幕上看到的配置）。发一次最小 chat 请求：测试模型 = 档案第一个 visible 模型；无 visible 模型时降级为模型列表 GET 探测；1 token 上限（openai/anthropic `max_tokens:1`，gemini `maxOutputTokens:1`）；超时 10s。结果 `ConnectTestResult`（延迟/HTTP 状态/错误摘要，仅展示用）。成功/失败按真实类型上报健康（401/403→auth、429→rate_limited、其他→server_error）。
- **FR-1.6 额度探测（usage probe）**：只读探测限流/配额响应头，返回 `UsageProbeResult { ok, httpStatus?, latencyMs, message, windows[], rawHeaders }`（window scope：5h/7d/30d/itpm/otpm/rpm/requests/tokens/unified/unknown）。**绝不写健康**。无配额信息时明示原因（可能不透传限流头；浏览器受 CORS）。
- **FR-1.7 删除被引用档案**：警告引用数，可强制删（节点回退 CLI 默认/解绑）。

### FR-2 Key 池调度与健康监控（P0）

- **FR-2.1 调度策略**：`weighted_round_robin`（Nginx 平滑加权，权重=流量比）、`priority_then_least_used`（默认；权重=优先级，同级取今日 tokens 最少）。
- **FR-2.2 绑定单位两种**：① 节点直连单档案（`providerProfileId`，不经调度，失败直接报错）——agent 节点只有这一种；② 隐式档案组调度池（组 = 同 presetId 启用档案；`custom` 中转每条自成一组）——llm_chat 未指定档案时走设置页「默认供应商组」。
- **FR-2.3 健康惩罚**：滑动窗口错误率；401/403 → 权重 0 + 拉黑 60s；429 → 临时降权 30s（按 `Retry-After` 延长）；5xx → 权重折半，5 分钟无错恢复。健康指标持久化，状态栏展示池健康。
- **FR-2.4 权重说明文案**（表单内固定展示）：
  > ▸ 加权轮询模式：按比例分流（权重 30 vs 10 → 流量 3:1）
  > ▸ 优先级模式：数字大者优先，同级时用得最少的先上
  > ▸ 出错自动降权惩罚，鉴权失败拉黑 60 秒

### FR-3 模型列表管理（P0）

- **FR-3.1 获取为主**：按协议调端点——openai 兼容 `GET {baseUrl}/models`；anthropic `GET {baseUrl}/v1/models`（**`x-api-key` 与 `Authorization: Bearer` 双认证头** + `anthropic-version`，最大化兼容各家）；gemini `GET {baseUrl}/v1beta/models?key=`（base 已含 `/v1beta` 不重复拼）。预设的 `modelsUrl/modelsAuth` 覆盖默认端点与认证方式。
- **FR-3.2 跨端点兜底**：anthropic 基座无 models 端点的（deepseek.com / moonshot.cn / moonshot.ai 的 `/anthropic`，与 presetId 无关、按 baseUrl 命中），跨到同族 openai 兼容端点拉模型（Bearer）。TS/Rust 同一张兜底表。
- **FR-3.3 业务错误体防护**：响应 2xx 但 body 无 `data`/`models` 数组且 `code≠0` 或 `success:false`（实测智谱无凭据返回 200 + code:1001）→ 按认证失败报错，**不得当成「获取到 0 个模型」**。
- **FR-3.4 获取参数**：超时 15s，失败不自动重试（toast + 保留旧清单 + 提示手动添加）；>500 条截断显示并提示用隐藏收敛。
- **FR-3.5 再获取合并规则**：manual 条目无条件保留；fetched 整体替换；id 仍存在的条目继承旧 visible 值；新条目 visible=true（不静默重置用户隐藏勾选）。
- **FR-3.6 手动兜底**：中转无 models 接口/返回不准时手填（`source:'manual'`）。
- **FR-3.7 消费单选**：节点的「模型」为单选下拉，选项 = 所绑档案 visible 模型；llm_chat 未绑档案维持手填 + 默认组聚合。「自动拉模型」时机：保存、连通测试时自动拉取。
- **FR-3.8 模型跟随**：切换档案时若当前模型不在新档案清单，自动切到新档案第一个可见模型（三处 Inspector 一致）；死模型名显式显示「不在列表，请重选」；llm 节点空模型**双侧前置报错**（不发起请求）。

### FR-4 可视化画布与节点系统（P0）

- **FR-4.1 节点清单（20 类）**：
  - LLM：`llm_chat`（流式打字机）、`llm_structured`（JSON Schema 强制 + 失败重试）
  - 生图：`image_gen`（gpt-image / grok）、`image_to_image`
  - 文件：`file_read`（UTF-8/base64 双模式）、`file_write`（覆盖/追加）、`glob_files`
  - 代码：`code_node`（JS）、`code_python`
  - 控制流：`condition`、`switch`、`for_each`、`while_loop`、`parallel`、`merge`（concat/zip/object 三策略）——DAG 内「值级算子」语义，无需子图执行
  - 工具：`prompt_template`、`http_request`、`delay`
  - 上下文：`context_compress`
  - Agent：`agent`（见 FR-8/9）
- **FR-4.2 强类型端口**：`text/image/file/json/number/any`，连线时类型校验（不兼容拒连 + 提示）；端口配色区分；一个输入端口默认只接一条入线（multi 端口如压缩节点 sources 可多接自动聚合）。
- **FR-4.3 数据传递两条规则**：① 端口连线（值优先——端口有输入时**优先于** Inspector 同名字段配置）；② 模板变量 `{{node_id.field}}`（点分路径，任意上游节点，不必直接相连）。文本框「变量」按钮列出当前可引用上游变量、点击插入。
- **FR-4.4 画布操作**：左侧节点库拖拽入画布 / 双击空白快速添加（推荐）/ 右键复制（配置一并复制）；右键菜单：复制/重命名/禁用（跳过执行）/**压缩并传递给…**（两节点间自动插入压缩节点）；滚轮缩放（0.15×–2.5×）、16px 网格吸附、小地图、自动保存（崩溃紧急快照恢复）、撤销重做。
- **FR-4.5 连线标签**：连线中点常显 `输出端口 → 输入端口` 标签 + 常显删除 `×`（无 × 时代码库曾长期没有删线入口——集成时勿重蹈）。
- **FR-4.6 快捷键**：`Ctrl+Enter` 保存并运行、`Ctrl+S`、`Ctrl+Z/Y`、`Ctrl+D` 复制、`Delete` 删除、`Ctrl+Shift+P` 命令面板、双击空白快速添加。

### FR-5 工作流执行引擎（P0）

- **FR-5.1 DAG 调度**：加载 JSON → 类型校验 → 构建 DAG → 拓扑分层，同层节点并发执行，逐层推进。
- **FR-5.2 节点状态机**：`idle → pending → running → (done | error | skipped | blocked)`；指数退避重试（默认 3 次，重试默认 5xx/429）；超时（默认 60s）；`skip_on_error`（失败不阻塞下游，下游标记 skipped 级联）；blocked 见 FR-9.4。
- **FR-5.3 运行控制**：▶ 运行（先保存）/ ⏸ 暂停 / 单步 / ■ 停止；单节点 Test Run（给端口输入 JSON 单独跑）。
- **FR-5.4 实时事件**：节点状态变更、流式输出 chunk、节点日志、run 终态（tokens/成本/耗时）推送到前端；流式按 16ms 节流批量推送防 IPC 风暴。
- **FR-5.5 实时渲染**：节点染色（绿=done/黄=running/红=error/灰=skipped）、连线流光、打字机输出。
- **FR-5.6 LLM 参数省键语义**：maxTokens/temperature **留空 = 请求不带该参数**（引擎双侧 None 省键，防 null 硬编码默认值）。
- **FR-5.7 Key 故障转移**：直连档案失败即报错；默认池调度时 auth/限流错误自动换档案重试。

### FR-6 上下文压缩（P1）

- 专用 `context_compress` 节点：sources（multi 端口）/ target_tokens（默认 2000）/ instruction（自定义摘要指令）；两种策略：LLM 摘要 / **提取式（零成本）**；token 节省统计。
- 右键「压缩并传递给…」一键在两节点间插入。
- 典型用途：多个大输出汇入一个下游 LLM 前先压缩，防超上下文窗口。

### FR-7 Agent Runtime 层（P0）

**数据模型**：

```ts
interface AgentRuntime {
  id: string;
  kind: 'claude-code' | 'codex' | 'grok-build' | 'dsh' | 'custom';
  displayName: string;
  installed: boolean;
  version?: string;
  executablePath?: string;    // 检测或手动指定
  installMethod?: 'npm' | 'binary' | 'manual';
  commandTemplate?: string;   // custom 专用
}
```

- **FR-7.1 四家内置 + 自定义**：claude-code / codex / grok-build / dsh 内置适配器元数据（bin 名、版本命令、安装命令、headless flags、注入通道），TS/Rust 双侧对齐；custom 为任意 CLI 登记（名称 + 可执行路径 + 命令模板）。
- **FR-7.2 检测**：跑版本命令探测 PATH；找不到允许 `setRuntimePath` 手动登记绝对路径（空串=清除恢复 PATH 检测）；版本超适配器兼容范围报「过新/过旧」。
- **FR-7.3 安装/更新**：npm 通道（claude/codex/dsh：`npm install -g <pkg>@latest`，无 npm 引导手动安装）+ **grok 直链双通道**（官方 npm `@xai-official/grok` 优先 → CDN/GCS 直链 `…/cli/grok-<ver>-<os>-<arch>[.gz|.exe]`，stable 指针解析版本，flate2 解压，彻底免 Node）；安装过程实时日志回传 Agents 页。
- **FR-7.4 custom 命令模板**：占位符 `{{prompt}}`（多行折叠单行入 argv，防 Windows BatBadBut）/ `{{model}}` / `{{env:VAR}}`（VAR 由所绑档案注入）；仅 env 注入，权限固定 workspace。

### FR-8 Agent 节点（P0）

**节点 schema**：

```ts
interface AgentNodeConfig {
  runtimeId: string;             // 必填
  providerProfileId?: string;    // 可空 = 用 CLI 自身默认配置
  model?: string;                // 单选，来自档案 visible 模型
  promptTemplate: string;        // 现有模板语法
  skillIds: string[];
  mcpServerIds: string[];
  permission: 'readonly' | 'workspace' | 'full';
  timeoutMs: number;             // 默认 300_000
  budgetUsd?: number;            // 仅 claude 原生支持；grok/dsh 无预算功能则忽略
  maxTurns?: number;
}
```

- **FR-8.1 绑定**：节点级绑定 runtime × 供应商档案 × skills × MCP servers（不做全局切换器）。agent 节点绑定档案只有「直连」一种方式。
- **FR-8.2 protocol × runtime 兼容矩阵**（硬约束，UI 层拦截 + 校验错误 + 运行期 blocked 三道闸）：

  | runtime | 接受的档案 protocol | 说明 |
  |---|---|---|
  | claude-code | `anthropic` | 只注入 ANTHROPIC_* 变量族 |
  | codex | `openai-compat` | 中转**必须支持 Responses API**（UI 提示） |
  | grok-build | `openai-compat` | GROK_MODELS_BASE_URL 通道是 chat/completions 线制 |
  | dsh | `openai-compat` | providers api 取 openai-completions 线制 |
  | custom | 任意（用户自负） | 仅 env 注入 |

  不兼容档案在下拉中**置灰可点** + tooltip 原因（看得到所以知道存在）。
- **FR-8.3 权限三档映射**：

  | 档位 | claude `--permission-mode` | codex | grok | dsh |
  |---|---|---|---|---|
  | readonly | `plan`/`dontAsk`+disallowedTools | `-s read-only` | `plan` | `read-only` |
  | workspace（默认） | `acceptEdits` | `-s workspace-write` | `acceptEdits` | 不设（headless 默认） |
  | full | `bypassPermissions` | `--yolo` | `bypassPermissions` | `danger-full-access` |

- **FR-8.4 各 runtime 执行命令与注入通道**（实测定值）：
  - **claude-code**：`claude -p <prompt> --output-format stream-json --verbose --model <m>? --settings '<env JSON>' --strict-mcp-config [--max-turns] [--max-budget-usd] --permission-mode <映射>`。供应商注入**必须走 `--settings` 的 env 块**（用户全局 settings 的 env 块会覆盖进程继承的同名环境变量，只 export 无效）；注入 `ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN`/`ANTHROPIC_MODEL`；模型已知时把 `ANTHROPIC_DEFAULT_*_MODEL` 全档位与 `CLAUDE_CODE_SUBAGENT_MODEL` 一并对齐到该模型（否则部分中转报「模型不存在」）。
  - **codex**：`codex exec --json --skip-git-repo-check -o <tmp> -C <workdir> -c model_providers.af_<id>.base_url=<url> -c …env_key=AF_KEY_<id> -c model_provider=af_<id> -c …wire_api=responses [-c mcp_servers.*] <prompt>`；key 经进程 env `AF_KEY_<id>`。
  - **grok-build**：`grok --prompt-file <tmp> --output-format streaming-messages-json [-m <model>] [--max-turns] --permission-mode <…>`；输出为 Anthropic 线格式（解析复用 claude 骨架，result 自带 total_cost_usd）；注入全走 env：`GROK_MODELS_BASE_URL` + `XAI_API_KEY` + 临时 `GROK_HOME=<workdir>/.af-grok-home`（`~/.grok` 零触碰）+ `GROK_DISABLE_AUTOUPDATER=1`；prompt 走 `--prompt-file`（规避 BatBadBut）。
  - **dsh**：`dsh --profile headless "<任务文本>"`；stdout 纯文本最终答案、退出码 0=completed；临时 `DSH_HOME` 写 settings.yaml（apiKeyEnv 引用 env 名），key 经 `AF_KEY_<id>`；权限经 `DSH_PERMISSION_MODE`；档案未绑时不设 DSH_HOME（CLI 用用户自己的 ~/.dsh）。
  - **custom**：按模板占位符执行，输出纯文本逐行 + JSON 结果形态探测；节点日志对密钥脱敏。
- **FR-8.5 成本回传分口径**：claude/grok 取 result 行原生 `total_cost_usd`/usage/modelUsage；codex/dsh 无 USD，取 tokens 按内置单价表折算（节点同时显示 tokens 与折算值）。
- **FR-8.6 并发纪律**：agent 进程**应用级全局信号量**（默认 3，可调 1–8），跨所有运行中工作流共享；与 provider 出站并发（16）相互独立；超限节点 pending + `queued` 徽标，FIFO。验收：并行 5 个 agent 节点，任一时刻进程数 ≤3。

### FR-9 Agent 节点执行语义（P0）

- **FR-9.1 前置检查**：runtime 已装且版本兼容 / 档案未禁用 / protocol×runtime 兼容 / custom 已登记——任一不满足 → **blocked**（不占并发槽）。
- **FR-9.2 临时注入**：所有注入物（供应商、skill、MCP）均为**本次执行的临时物**（`--settings`/`--mcp-config`/`-c` 覆盖/临时 `*_HOME`/workdir 布局），**绝不写用户全局配置**（`~/.claude`、`~/.codex`、`~/.grok` 等）；临时文件用完即删。
- **FR-9.3 流式执行底座**：`run_process_streaming`（逐行回调 + 环形缓冲限流 + 超时/取消骨架）；取消/超时**杀整棵进程树**（Windows `taskkill /T /F` / Unix `killpg`）。
- **FR-9.4 blocked 状态机**：blocked 视同结构性跳过——下游 skipped（复用 skip_on_error 传播）；run 终态 partial_fail（全 blocked 则 error）；blocked 节点输出端口 = 空串（下游模板渲染为空不报错）；浏览器模式整图运行前弹确认框列出将 blocked 的 agent 节点（继续=agent→skipped / 取消）。
- **FR-9.5 失败分类**：进程非 0（解析 stderr + is_error）、超时（杀树）、鉴权失败（指向档案）；节点 failed 但**保留已收到的流式输出**。

### FR-10 Skills 子系统（P1）

- **FR-10.1 扫描根**（桌面）：`~/.claude/skills`、`~/.agents/skills`、项目 `.claude/skills`、项目 `.codex/skills`；Skill = 目录内 `SKILL.md`（frontmatter name/description）。
- **FR-10.2 自建库**：应用数据目录 `skills/<name>/SKILL.md` + 内置编辑器（name/description/body）；浏览器侧存 OPFS；两引擎持久化各管各的。
- **FR-10.3 节点勾选按次注入**：
  - claude：勾选 skill 复制/软链进 workdir `.claude/skills/` + `--settings '{"skillOverrides":{…}}'` 按次限定（未勾选全 off）
  - codex：workdir `.codex/skills/` 布局
  - grok：workdir `.grok/skills/` + `GROK_CLAUDE_SKILLS_ENABLED=false`（已知限制：用户全局 `~/.agents/skills` 是 grok 原生根，无按次关闭通道，注入集之外仍可见）
  - dsh：workdir `.agents/skills/`（目录名 slug 化）
  - custom：不注入
- **FR-10.4 Skills 页**：按来源分组列表 + 兼容 runtime 标签 + 刷新扫描 + 自建编辑器。

### FR-11 MCP 子系统（P1）

- **FR-11.1 McpServer 模型**：`{id, name, transport: 'stdio'|'http', command/args/env | url, enabled}`；MCP 页 CRUD + 启停，双引擎均可管理配置。
- **FR-11.2 节点勾选按次注入**：claude `--mcp-config <临时 json>`（远程条目必须带 `type`）+ `--strict-mcp-config`（**安全必须**：-p 模式对项目 `.mcp.json` 不弹信任提示）；codex `-c mcp_servers.<name>.*`；grok/dsh/custom 勾选时警告忽略。
- **FR-11.3 语义**：**模型自主调用**（把 server 工具挂给 agent，推理中自行决定），不做编排层强制调用的 MCP 节点；工具调用细节不解析，只在节点日志展示；MCP 启动失败 `mcp_server_errors` 透传节点日志（警告不阻塞）。

### FR-12 运行调试与终端（P0）

- **FR-12.1 内置终端**（替代传统 run log）：底部终端面板实时滚动每个节点的过程与输出；点节点卡「打开终端」聚焦该节点；历史会话可切换。
- **FR-12.2 终端边界（已声明，用户接受）**：**观察窗非交互式**（无 stdin 双向通道，明确不要交互式输入）；关闭终端不影响执行；软件层可终止；**跨重启只留最终输出**（过程日志不落库）。
- **FR-12.3 统计**：tokens/费用/耗时（单价估算表，TS/Rust 双侧同表）。
- **FR-12.4 单节点 Test Run**：见 FR-5.3。

### FR-13 UI / UX 需求（P1）

- **FR-13.1 页面结构**（侧栏）：工作流 · 供应商 · Agents · Skills · MCP · 媒体 · 设置（设置含「默认供应商组」「agent 并发上限」）。
- **FR-13.2 设计 token 驱动**：深浅双主题；**透明度一律走 token**（玻璃 `af-glass`/遮罩 `bg-scrim-*`/表面 `bg-surface-*`），禁止裸 rgba（`pnpm check:alpha` 把关）；规则：玻璃只用于浮层（Modal/菜单/命令面板/悬浮面板），内容承载区一律不透明；同屏 blur 层数 ≤2。
- **FR-13.3 供应商编辑两步式**：① 预设选择器（搜索 + 分类 chips + 预设卡网格 + 自定义虚线卡）→ ② 编辑表单（玻璃 Modal）。
- **FR-13.4 Agents 页**：四张 runtime 卡（版本/可更新徽章/检测/安装/更新 + codex 的 Responses API 提示）+ 自定义登记虚线卡 + 实时安装日志。
- **FR-13.5 概念必须带中文说明**：新概念（userprompt/systemprompt/temperature/权重/连线语义…）必须带说明行/hint，讲清「对模型来说最终变成什么」；误导性/冗余控件坚决删（如 llm_chat 的 Provider 下拉已删——选档案协议自然体现）。
- **FR-13.6 画布渲染**：节点卡显示 Provider 徽章/输入摘要/流式输出预览/端口；状态动效（running 呼吸、done 脉冲、error 抖动、连线流光）；`prefers-reduced-motion` 降级。
- **FR-13.7 其他**：命令面板（Ctrl+Shift+P）、状态栏 Key 池健康、媒体画廊（只收 `media/`）、欢迎页最近工作流 + 示例模板（模板**不硬编码任何厂商/模型**）。

### FR-14 数据持久化与迁移（P0）

- **FR-14.1 存储**：桌面 SQLite（workflows/runs/node_runs/settings/provider_profiles/skills/mcpServers…）+ keyring 存密；浏览器 localStorage + OPFS 对称。
- **FR-14.2 旧数据幂等迁移**：`api_keys`（6 值 ProviderId 枚举）→ `provider_profiles`，显式映射表（glm→zhipu-glm、fable→custom+anthropic…），`providerMigratedAt` 标记防重复；验收 = 迁移前后各启动一次，行数与字段完全一致。
- **FR-14.3 自动保存**：编辑器内自动保存，离开落盘，崩溃紧急快照恢复。

### FR-15 导入导出（P1）

- `.aflow` 工作流导出/导入（纯 JSON，可版本管理，双模式通用）；导出与全量备份**自动剔除 `secret/key/token` 字样字段**。

---

## 4. 非功能需求

### NFR-1 安全（P0）

| 项 | 要求 |
|---|---|
| Key 明文 | 桌面仅存 OS keyring（secret-service/wincred），请求时取出注入 HTTP 头，**不落盘不打印**；浏览器存 localStorage（状态栏警示） |
| 注入隔离 | agent 执行的一切注入均为临时物，用完即删，**绝不写用户全局配置** |
| 导出脱敏 | 自动剔除 secret/key/token 字段 |
| HTTP 出站 | `http_request` 节点默认仅允许 6 家 Provider 域名白名单，其余设置中手动加白 |
| 代码沙箱 | 浏览器 JS=Web Worker（无 DOM）、Python=Pyodide WASM；桌面=本地子进程（超时强杀、输出限 1MB） |
| 文件访问 | 相对路径锚定数据目录，`..` 归一化防越界 |
| MCP | claude 必带 `--strict-mcp-config` 收口 |
| CSP | 维持域名白名单不放宽（桌面出站全走 Rust reqwest 不经 webview）；保留 `ipc: http://ipc.localhost` |
| 日志脱敏 | custom runtime 节点日志对密钥脱敏 |

### NFR-2 性能（P1）

| 指标 | 目标 |
|---|---|
| 画布 60fps @ ≤200 节点 / 30fps @ ≤500 节点 | 必须 |
| 启动到可交互 | < 1.5s |
| 流式首字延迟 | < 200ms（Provider 决定） |
| 节点并发 | 同时 32 个 LLM 请求；agent 进程全局 ≤3（可调 1–8） |
| 流式推送 | 16ms 节流批量，防 IPC 风暴 |
| 内存 | 基线 <100MB，运行 100 节点 <500MB |

### NFR-3 可靠性与错误处理（P1）

- 永不 panic 跨 IPC；错误消息用户友好（如「网络超时，请检查代理」），原始错误链写日志。
- 取消语义：CancellationToken 注入每个节点任务，agent 节点杀整棵进程树。
- 报错分流（用户手册口径）：404=端点配错；400 Model Not Exist=旧模型名；401=key；网络错误=代理/GFW。
- 节点 failed 保留已收到的流式输出。

### NFR-4 兼容性（P1）

- 平台：Windows x64（主力）、Linux x64/arm64；Tauri 2 + WebView2。
- 系统代理：桌面 HTTP 栈支持系统代理（reqwest system-proxy 特性，Clash 等场景实测必需）。
- 浏览器模式已知限制：部分 Provider 受 CORS（桌面无此问题）；agent 节点/skills 扫描/runtime 安装不可用（引导态/blocked）。

### NFR-5 UI 规范（P1）

- 设计 token 驱动（颜色/间距/圆角/动效），深浅双主题，WCAG 2.1 AA；透明度 token 化 + lint 把关（见 FR-13.2）。

### NFR-6 测试基线（P0）

- Rust core：单元 + 端到端测试无网络依赖（本项目基线 120+8 通过）；网络集成测试 `#[ignore]` 环境变量门控。
- TS：typecheck、Playwright E2E（本项目基线 42/42，浏览器模式覆盖供应商 CRUD/节点编辑/Skills/MCP 页/拖拽/模型跟随）。
- 双引擎一致性：实体以 TS 类型为源，Rust serde 对齐 + 镜像用例。
- 双目标（TS/Rust）编译 0 警告。

### NFR-7 打包分发（P1）

- CI 矩阵：Linux x64/arm64 + Windows x64，push main 出 Artifacts；`v*` 标签出 Draft Release（msi/exe/nsis/AppImage/deb）。
- Linux CI 构建启用 GNOME Keyring/KWallet 后端。

---

## 5. 集成必须保留的架构不变量

移植到另一个项目时，以下不变量破坏任何一条都会导致语义分叉或安全事故：

1. **Backend 接口隔离**：UI 只依赖统一 `Backend` 抽象接口，双引擎（浏览器 TS / 桌面 Rust）可替换实现。
2. **TS/Rust 同语义双侧同步改、双侧测试**：数据模型、调度、执行语义两引擎一致；纯函数（joinUrl/maskSecret/兜底表/单价表）双侧镜像。
3. **绝不写用户全局配置**：agent 供应商/skill/MCP 注入全为按次临时物（--settings/-c/临时 HOME/workdir 布局），用完即删。
4. **临时注入优先于继承环境**：claude 必须走 `--settings` env 块（用户全局 env 块会覆盖进程环境）；codex 切中转只能走 `-c model_providers`。
5. **杀整棵进程树**：agent 子进程取消/超时必须杀树（taskkill /T /F、killpg），只杀直接子进程会留孤儿。
6. **blocked 是一等节点终态**：浏览器 agent 不可执行要「明确提示 + 可选继续」，不是静默失败。
7. **密钥不出现在列表接口**：列表只回掩码，明文按需 reveal；导出自动脱敏。
8. **Windows 拖放**：Tauri 桌面要 `dragDropEnabled:false`，否则 WebView2 OLE 抢占 HTML5 DnD（左侧节点库拖拽失效的根因）。
9. **桌面形态 Tauri**：Electron 方案已明确否决，勿再引入。
10. **实测依据写进模块头注释**：runtime 适配的每个 flag/注入通道都标注实测版本与来源，后人改动先读注释。

---

## 6. 明确不做的事（边界）

- 不做 cc-switch 式「写全局配置的切换器」（只做节点级按次注入）。
- 不做编排层强制调用的 MCP 节点（MCP = 模型自主调用）。
- 不自研 function calling（skill/MCP 全经 CLI 原生配置注入）。
- 不做断点续跑（失败修正后整图重跑）。
- 不做交互式终端（终端是观察窗，无 stdin 双向通道）。
- 过程日志不跨重启落库（只留最终输出）。
- 不做用户自选调度集合（绑定单位只有「单档案直连」与「隐式档案组」两种）。
- 工作流市场/团队协作/插件系统/移动端/本地模型（Ollama 等）/cron 触发——后续扩展，当前范围外。

## 7. 已知限制（如实告知集成方）

- Token 单价为内置估算表，与实际账单可能有出入。
- 控制流表达式是 JS 子集（算术/比较/逻辑/属性访问/三元），非完整 JS。
- 桌面 `code_python` 走本地 `python3` 子进程（wasmtime+Pyodide 为原设计后续路线）。
- grok 无法按次关闭用户全局 `~/.agents/skills`（1.0.13 无通道）。
- dsh 适配是 tarball 源码级核实（0.1.1-rc.2），部分枚举待实机复核。
- 浏览器模式 CORS 视各家 Provider 策略。
- Linux arm64 CI 依赖 GitHub 原生 arm 运行器（私有仓库会跳过该条目）。

---

## 8. 验收标准（摘要）

**供应商与调度**：自定义中转档案能建/能测/能被 llm_chat 与 agent 节点使用；迁移幂等（前后各启动一次行数字段一致）；5 并发 agent 时 headless 进程数 ≤3。

**Agent 执行**：claude + Anthropic 协议中转 key headless 跑通，节点显示流式输出与成本；grok/dsh/custom 真跑；取消/超时进程树清零。

**Skills/MCP**：节点勾选的本机 skill 与自建 skill 执行中生效（claude init 行 skills 字段可见）；勾选 MCP server 注入后模型能自主调用其工具（节点日志可见）。

**画布**：左侧拖拽入画布（Windows 实机）；弹窗可读（透明度整改后）；连线可删（常显 ×）；切档案模型跟随。

**报错前置**：llm 空模型在发起请求前报错，不浪费一次 API 调用。

---

## 9. 术语表

| 术语 | 定义 |
|---|---|
| ProviderProfile 供应商档案 | 一条「预设/协议 + baseUrl + key + 模型清单」的组合，同预设可多档案 |
| 调度池 / 档案组 | 同 presetId 的启用档案集合（custom 中转每条自成一组），llm_chat 未绑档案时按组调度 |
| Runtime / Agent Runtime | 本地 agent CLI 的适配实体（claude-code/codex/grok-build/dsh/custom） |
| headless | CLI 的无界面一次性执行模式（-p / exec / --profile headless） |
| blocked | 节点终态：运行前校验失败（未装/禁用/不兼容/浏览器模式），结构性跳过 |
| skip_on_error | 节点失败时下游标记 skipped 而非整图失败 |
| workdir | agent 节点执行时的沙箱工作目录（`agent-runs/<运行>-<节点>/`），注入物落于此 |
| .aflow | 工作流导出文件格式（纯 JSON，脱敏） |

---

*文档版本 1.0 · 2026-08-29 · 基于 AgentFlow v0.1（`768bba9`）整理*
