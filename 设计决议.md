# herdr × AgentFlow 融合设计决议

> 2026-08-29 · 三轮设计拷问后锁定 · baseline commit `c2637dc1` · 待最终确认后生效

## 0. 一句话

在 herdr fork 内深度融合：**节点 = pane、运行 = workspace、编排 = herdr 原生 TUI 视图**，provider 档案系统解决"每个节点不同的模型/Key/地址"，DAG 引擎 + 内容哈希缓存解决"可监控、可中断、改图再续"。

## 1. 架构总则（不可动摇）

| # | 决议 |
|---|---|
| A1 | 改 fork 源码（HuuuRJ/herdr），贴边加法：新模块 `src/provider/`、`src/workflow/` 为主，既有文件仅动触点清单（main/events/state/api/ui/config/键位，共 ~9 处小改） |
| A2 | 单 Rust 引擎（herdr server），砍双引擎/浏览器模式；无 GUI 画布，TUI 图视图 + inspector 承担总览与编辑 |
| A3 | UI 100% 沿用 herdr 设计语言（Mode/modal/sidebar/Palette/键位习惯） |
| A4 | v1 无 HTTP 栈：所有出站请求（连通测试/模型拉取/**生图**）走 curl 子进程（herdr 惯例，update.rs 同款）；P3 直连 LLM 节点时再评估 reqwest |
| A5 | 密钥：config 目录 0600 独立文件；API 列表只回掩码，按需 reveal；不迁移动 AgentFlow 存量数据 |
| A6 | 注入纪律（AgentFlow 不变量 3）：一切供应商/skills/MCP 注入均为按次临时物（--settings / -c / 临时 HOME / workdir 布局），绝不写用户全局配置；与 herdr 自家 integration 钩子互不干涉 |
| A7 | 引擎驱动执行全走 herdr 既有原语：LayoutPane(command+env+cwd) / worktree / pane.read / send-input / PaneDied / platform 杀树；不建平行执行路径 |

## 2. Provider 档案子系统（P0）

| # | 决议 |
|---|---|
| P1 | 预设表全量搬 AgentFlow 17 家（含国内中转/Coding Plan/自定义）；协议是档案属性——同一供应商双协议 = 两条档案 |
| P2 | v1 只做**节点直连档案**绑定；池调度/加权/健康惩罚/failover = P3 |
| P3 | 连通测试 = curl 最小 chat（1 token 上限，10s 超时）；模型列表 = curl GET；失败不自动重试 |
| P4 | 验收集（真实 key）：claude-code ×（GLM / Kimi / DeepSeek / Opus+Fable 中转）；codex ×（GPT / GLM / Kimi / DeepSeek）；生图 ×（xAI + OpenAI，都用） |
| P5 | TUI 管理界面进 herdr settings 体系（列表 + 表单弹窗，掩码 + reveal + 留空保留原 key） |

## 3. 工作流引擎语义（P1）

| # | 决议 |
|---|---|
| W1 | 工作流文件 = 任意路径 `.aflow.json`（含节点坐标字段，为将来任何图形化留位）；TUI 记最近列表 |
| W2 | v1 节点集：`agent` + `prompt_template` + `image_gen`；控制流（condition/for_each/parallel）与工具节点 = P3 |
| W3 | 节点输出 = 结构化轻量：`output` = 最终答案文本 + `.model` / `.cost_usd` / `.tokens` 可选字段；过程输出留在 pane，不进数据流 |
| W4 | 输出捕获 = pane 内 `\| tee <run-dir>/output`（看得见 + 文件干净副本）；节点可 `visible:false` 走 detached 后台 |
| W5 | 完成判定 = 进程退出（PaneDied）为主 + 超时兜底；herdr agent 检测（working/blocked/idle）只用于 UI 染色，不进执行语义 |
| W6 | 超时默认 0 = 不超时（字段可设）；自动重试默认 0（agent 重跑烧钱，重试显式声明） |
| W7 | 并发 = server 级全局信号量，默认 3，config `[workflow].max_concurrent_agents` 可调 1-8，跨所有 run 共享 |
| W8 | 中断两档：`pause` = 软停（在跑节点跑完、不派新节点、可改图可 resume）；`cancel` = 硬停（杀进程树、缓存保留）；丢弃缓存 = `workflow delete`，不设第三档 |
| W9 | resume = 内容哈希缓存：键 = (node_id, 节点配置哈希, 上游输出哈希)；中断后增删/修改节点 → 未变节点直接跳过；失败修正后重跑只重跑受影响子图 |
| W10 | run = 独立 workspace（`wf-<工作流名>-<序号>`）；节点 pane 顺序 split，pane label = 节点名；断电/重启随 herdr 会话恢复（agent_resume 白送）；`workflow delete` 连 workspace 一起清 |
| W11 | run 数据 = `state_dir/workflows/runs/<run_id>/`（输出缓存 + 元数据）；默认保留最近 20 个自动清理，config `[workflow].keep_runs` 可调 |
| W12 | prompt 模板在文件里编辑；TUI inspector 只改单值绑定字段（runtime / 档案 / 模型 / 超时 / visible / enabled） |
| W13 | meta 编排不加专门机制：API 与 CLI 同套天然可达；P1 验收一条"agent 在 pane 里触发 workflow + wait" |

## 4. CLI 适配排期（注入通道全部照 AgentFlow 实测结论）

| 阶段 | 适配器 | 要点 |
|---|---|---|
| P1 | **claude-code** | `--settings` env 块注入（ANTHROPIC_BASE_URL/AUTH_TOKEN/MODEL + DEFAULT_*_MODEL 全档对齐）；`-p --output-format stream-json`；`--strict-mcp-config` |
| P1 | **codex** | `-c model_providers.af_<id>.*` + `wire_api=responses`；key 经进程 env `AF_KEY_<id>`；`--json` |
| P1 | **custom** | 命令模板 `{{prompt}}/{{model}}/{{env:VAR}}`；多行折叠单行入 argv 防 BatBadBut；日志脱敏 |
| P2 | **grok-build** | env 注入（GROK_MODELS_BASE_URL/XAI_API_KEY/临时 GROK_HOME）+ `--prompt-file`；输出 Anthropic 线格式复用 claude 解析骨架 |
| P2 | **dsh** | 临时 DSH_HOME 写 settings.yaml；`--profile headless`；DSH_PERMISSION_MODE |

## 5. image_gen 节点（P1 起）

| # | 决议 |
|---|---|
| I1 | 只实现 **openai-compat images 端点**一种协议（覆盖 xAI grok image / OpenAI gpt-image / 任何兼容中转）；curl 可达，不破 A4 |
| I2 | P1 = 落文件（run 目录），节点输出给路径；P2 = 经 herdr pane graphics（kitty 协议）把生成图直接投到节点 pane 显示 |
| I3 | image_to_image = P3 |

## 6. TUI（P2）

- 新 Mode：workflow 图视图（ASCII DAG + 状态染色 + 方向键导航 + Enter 跳节点 pane + `i` inspector）
- sidebar：run 列表入口；节点状态叠加 herdr 既有 agent 检测显示
- 复用 herdr modal/settings/键位体系，零新 UI 概念

## 7. 分期与验收

| 阶段 | 内容 | 验收 |
|---|---|---|
| **P0** | provider 档案：模型 + 0600 密钥 + 预设表 + API + settings 页 + curl 连通测试/模型拉取 | 2-3 家真实档案建→测→拉模型全通；列表只见掩码 |
| **P1** | 引擎 + claude/codex/custom + image_gen + 缓存 resume + pause/cancel + CLI | 多节点工作流不同节点不同档案真实跑通；tee 捕获；pause→改图→resume 缓存跳过；cancel 进程树清零；并发 ≤3；agent-in-pane 触发 workflow；生图落盘 |
| **P2** | TUI 图视图 + inspector + grok/dsh 适配 + kitty 投图 | 图上导航/跳 pane/inspector 换绑定即改即跑；grok/dsh 真跑；生成图 pane 内显示 |
| **P3** | 控制流/工具节点、池调度/failover、直连 LLM 节点（评估 reqwest）、image_to_image | 届时另定 |

## 8. 开发纪律

- 改任何既有符号前 `codegraph impact` 查爆炸半径；每步 `just lint` + `just test`（Windows 走 `scripts/windows_check.ps1`）
- 遵守 herdr AGENTS.md：AppState 不膨胀（新子系统独立注册表）、render 纯读、平台代码只进 platform/、乘法路径纪律（改热路径跑 bench-render-scale）
- 非测试零 unwrap、生产无 unsafe
- 新增 Method 记得三处同步（schema / app/api.rs 分发 / api/mod.rs request_changes_ui）
- upstream merge 策略：锁 baseline 开发，功能成型后按触点清单重放跟进

## 9. 明确不做

GUI 画布（含 Tauri/Electron/Web 壳）、双引擎、浏览器模式、AgentFlow 存量迁移、编排层强制调用的 MCP 节点、自研 function calling、交互式终端编辑器以外的任何第二前端。
